#include <stdio.h>

struct my_struct {
    int number;
    char letter;
    double another_number;
}

int main(void) {
    // int num;
    // int input;
    // scanf("%d", &input);
    // arr[input-1] = 'c';
    
    struct my_struct *strct = malloc(sizeof(struct my_struct));
    free(arr)
}

